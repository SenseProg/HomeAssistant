# Реєстр інцидентів HomeMate

## Навіщо

Розбір недоливу 10 серпня 2026 показав дірку в процесі: знахідка живе рівно
стільки, скільки триває розмова. Асистент кілька разів наново «відкривав» те, що
вже було з'ясовано, а проблеми, які не полагодили одразу, просто зникали з поля
зору.

Реєстр — це список **відкритих технічних проблем із доказами**. Він навмисно
відокремлений від пам'яті: пам'ять тримає побажання власника («бойлер гріємо
вночі»), реєстр — те, що зламано і що треба полагодити.

## Де лежить

`/userdata/hass/config/.private/claude-code-conversation/incidents.jsonl`,
права `0600`, поруч з історією діалогу й пам'яттю. Поза `.storage`, поза
репозиторієм, поза резервними копіями HA. Запис атомарний через тимчасовий файл,
обмеження — 200 записів і 1500 символів на текстове поле. Ліміт піднято з 600
після першого ж змістовного запису: у `resolution` має вміщатися спосіб
виправлення з перевіркою, а не його початок.

Поля: `id`, `created`, `updated`, `status`, `severity`, `area`, `title`,
`detail`, `evidence`, `resolution`, `source`.

| Поле | Значення |
|---|---|
| `status` | `open` — треба лагодити; `watching` — полагоджено, чекаємо підтвердження; `resolved` — закрито |
| `severity` | `high`, `medium`, `low` |
| `area` | `irrigation`, `energy`, `climate`, `network`, `board`, `assistant`, `other` |
| `source` | `assistant` — завів агент у чаті; `operator` — обслуговча сесія або власник |

Невідоме значення `severity` чи `area` не ламає запис, а тихо падає у `medium`
і `other` — модель не мусить пам'ятати перелік напам'ять.

## Для асистента

**Читання без запиту.** У кожне повідомлення додається блок `<known_incidents>`
з незакритими інцидентами (до 12, спершу `open`, потім за тяжкістю). Агент має
звірятися з ним і не подавати відому проблему як нову знахідку.

**Інструменти** доступні незалежно від дозволу на керування — це файл, а не
пристрої:

```json
{"tool_call": {"name": "IncidentList", "arguments": {"status": "all"}}}
{"tool_call": {"name": "IncidentAdd", "arguments": {"title": "…", "detail": "…", "evidence": "…", "area": "irrigation", "severity": "high"}}}
{"tool_call": {"name": "IncidentUpdate", "arguments": {"id": "cac99963", "status": "resolved", "resolution": "…"}}}
```

**Команди в чаті** обробляються локально, без запуску CLI:

```text
/incidents                          відкриті
/incidents all | resolved | watching
/incidents close <id> <чим закрито>
```

**Коли заводити.** Відтворювана технічна проблема: розбіжність очікуваного й
фактичного, недолив або перелив, пристрій `unavailable` довше за випадковий
збій, помилка в логах, що повторюється, показник, який суперечить іншому.

**Коли не заводити.** Разове питання, побажання власника (це пам'ять), те, що
вже є у переліку, власна невпевненість. Одна проблема — один запис.

**Поле `resolution` заповнюється не тільки при закритті.** Щойно спосіб
виправлення відомий, він записується туди — з конкретними сутностями, кроками
й перевіркою, за якою видно, що подіяло. Запис без способу лише констатує
проблему і змушує наступну сесію розбиратися заново. Якщо спосіб відомий, але
ще не застосований, це прямо позначається на початку тексту.

## Для обслуговчої сесії

```bash
python mcp-server/cli.py incidents
```

Той самий перелік дає MCP-інструмент `ha_incidents` з фільтром `open` (типово),
`resolved` або `all`. Інструмент лише читає — заводити й закривати записи можна
з чату або скриптом на платі.

## Що вже в реєстрі

Зріз із плати на 02.09.2026 (`python mcp-server/cli.py incidents all`); ця
таблиця — довідка, джерело правди лишається файл на платі.

| id | Статус | Що це |
|---|---|---|
| `cac99963` | watching | Нічний полив недоливав: контролер закривав клапан за власним відліком. Таймер DP пишеться перед `manual_run` з 10.08; нічні прогони з того часу без обривів |
| `b28ecf5b` | open | Irrigation Unlimited не помічає, що клапан закрився посеред поливу. Сторож `irrigation_watchdog_master_without_valve` уже стоїть з 22.08 — запис можна закривати після першого підтвердженого спрацювання або просто як реалізований |
| `41cbad84` | watching | ФС `/userdata` пошкоджена. fsck 22.08 допоміг на тиждень: помилки повернулись 30.08, на ранок 03.09 їх 17. Корінь — ext4 без журналу і знеструмлення плати. 03.09 09:06: e2fsck, увімкнено журнал, `errors=remount-ro`, `nofail`, fsck на завантаженні (`fsck-userdata.sh` v2). Закрити після першого знеструмлення без нових помилок |
| `07c087e7` | open, high | Гідроакумулятор не працює: насос свердловини стартує вчетверо частіше за норму. Фізична перевірка тиску в баку |
| `8805e616` | open | Статистика `sensor.pidloha_1_za_tarifom_nich` без одиниці (kWh → None). Лікується `recorder/update_statistics_metadata` з `unit_class: energy` |
| `0910ac67` | open | Сушарка на панелі «Енергія» посилається на неіснуючий хмарний сенсор; потрібен локальний інтегратор енергії |
| `0cfd77b0` | resolved | Smart Irrigation не зараховував полив зонам 1 і 2 — зони були прив'язані до хмарних дублів клапанів; виправлено 11.08 |
| `5e06a539` | resolved 02.09 | Хмара Tuya: третій акаунт не автентифікувався і дублював пристрої; запис видалено |
| `7a1f4e02` | resolved 02.09 | Не було `/home/forlinx/.ha_token`: три інструменти мертві, невдалі запити банили localhost |
| `4d2c258d` | resolved 02.09 | `origin/main` розійшовся з робочою гілкою; злито без втрати автоматизацій |
| `633345ee` | resolved | Розетка насоса віддавала один семпл на цикл — причиною був незаданий `scan_interval` LocalTuya, виставлено 1 с 22.08 |
| `63bfcabf` | resolved | Графік «спожито з мережі по 5 хв» показував піки до 14 кВт — артефакт джерела |
| `64282c17` | resolved | Картки води додавали весь інтеграл у стовпчик дня рестарту (5 148 л замість 618 л); виправлено 30.08 |
| `cdc7d7c2`, `791ada47` | resolved | Сирий HTML у поясненні Smart Irrigation; відеокліпи падали на неіснуючому `shell_command` |
| `a59e5df1` | watching 03.09 | Плата знеструмилась ~12:50–13:58 (разом із розеткою насоса .26; логер Deye, PRANA, Blauberg теж моргали — схоже, просів вихід інвертора; лічильник, роутер, NAS жили). Стартувала з годинником 2024: 1047 рядків states з датою 2024, токен «ще не чинний» → бан 127.0.0.1 у `ip_bans.yaml`. Зроблено: drop-in `wait-for-clock.conf` для home-assistant.service, запис бану прибрано з файлу (пам'ять — до рестарту), `deploy.sh reload` з запасною LAN-адресою. Відкрито: причина просідання живлення плати; ФС після знеструмлення `clean` (журнал спрацював) |
| `52016a12` | watching 03.09 | Нічний бекап HA з 11.08 щоночі обривався (`securetar.AddFileError`: кліпи під `media/video` і `www/motion-clips` в архіві, 5–9 ГБ, файл зникає посеред tar; 03.09 — мертвий `-shm` зламаної копії minute_rollup). Жодного завершеного бекапу з 10.08, ретенція не працювала: 26 архівів, 127 ГБ на NAS. Відео винесено поза дерево конфігу (`finish-video-move.sh`, чекає рестарту власника), галерея — через підписані media-URL. Перевірка: архів 04.09 має важити десятки МБ і `last_completed_automatic_backup` зрушити |

## Пов'язане

- [assist-history-access.md](assist-history-access.md) — доступ агента до
  історії, атрибутів і інструментів читання.
- [assist-exposed-entities.md](assist-exposed-entities.md) — межа того, чим
  агенту дозволено керувати. Реєстр її не розширює.
