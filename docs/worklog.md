# Журнал робіт HomeMate

Хронологічний журнал того, що знайдено і що зроблено в системі. Один запис на
робочу сесію (день), найновіше зверху. Це не реєстр інцидентів
([incident-register.md](incident-register.md) тримає відкриті технічні проблеми
з доказами) і не пам'ять асистента (побажання власника): тут — **знахідки,
рішення і зміни з посиланнями на коміти**, щоб будь-яка наступна сесія бачила,
що вже зроблено і чому.

## Як вести

- Запис робиться в кінці сесії або після кожної суттєвої зміни; коміт журналу
  йде разом із рештою змін.
- Структура запису: **Знахідки** (що з'ясувалось, з числами), **Зроблено**
  (з комітами), **Рішення власника** (дослівно, з датою), **Чекає** (що не
  закрито і хто має закрити).
- Те, що вже є в коді або в Git, не переписувати — посилатись.

---

## 2026-09-03

**Знахідки**

- Recorder: 3,87 млн рядків `states` за 7,3 доби, файл 1,24 ГБ (385 МБ вільних
  сторінок), ~110 МБ/добу справжніх даних; 42 % рядків писали сутності, яких
  немає на жодному дашборді (дублі Deye Load UPS / Internal CT / Output /
  Power Losses / зовнішні CT, `inverter_connection` і `inverter_update_interval`
  з атрибутами кожні 5 с, напруги Sonoff «Ворота. Гараж.» — 214 тис. рядків,
  хмарні дублі локальних `_2`). Погодинна статистика зберігається безстроково
  (8 МБ за 39 діб), 5-хвилинна і сирі стани — разом із `purge_keep_days`.
- Бекапи HA: Core архівує все дерево конфігу разом із маунтами (виключення лише
  `backups/*.tar`, логи, `tts/*`, `.cache/*`), думка «media/* не в архіві» була
  хибна. Кліпи камери лежали в `media/video` (NFS) і в bind `www/motion-clips`:
  архів 5–9 ГБ, обривався посеред tar (`securetar.AddFileError`),
  `last_completed_automatic_backup` завис на 10.08, ретенція 7 копій не
  працювала — 26 архівів на 127 ГБ на NAS, 21 із них обірваний. Файл, що
  зламав tar 03.09: мертвий `-shm` копії `minute_rollup.db.broken-20260828`.
- `/local/` не віддає symlink, що резолвиться поза `www/`; media source віддає
  файли за підписаним URL (`media_source/resolve_media`, чинний 24 год) і його
  можна викликати з iframe того самого origin через `hass` батьківського вікна.
- Сторінка «Свердловина» помилялась у розрахунках (див.
  [charts-audit-2026-09-03.md](charts-audit-2026-09-03.md)): картки читали сиру
  історію глибиною 7 діб як «30 днів», коефіцієнт брався на момент
  спрацювання, прапорець насоса лип після рестарту.
- Блекаут 10:33–10:35 (справжнє блимання мережі, 0 В на інверторі й
  лічильнику): автоматика вимкнула бойлер Midea, насос свердловини, сушарку,
  малу пральну, PRANA, Blauberg і за задумом не вмикала назад — лише
  нагадування. Бойлер бабусі в списку не був і грів за розкладом 04:00–06:59.
- Три різні «споживання» на «Блекауті»: hero (markdown) відстав від стану і
  показував 1,55 кВт / 5,9 год, плитки з того самого сенсора — 0,86 кВт /
  10,7 год, шкала — 0,97 кВт весь дім за лічильником (`zagalne_navantazhennia`
  — UI-helper, формули в YAML немає).
- Плата знеструмилась ~12:50–13:58 разом із розеткою насоса `.26`; логер Deye,
  PRANA, Blauberg моргали (ймовірно просів вихід інвертора; лічильник, роутер,
  NAS жили). Після старту RTC = 2024-06-17, HA піднявся раніше за NTP: 1047
  рядків states з датою 2024, токен виглядав «ще не чинним», п'ять невдач —
  `http.ban` забанив 127.0.0.1. Симптом: 403 на кожен localhost-запит при
  робочому браузері; LAN-адреса `192.168.50.141:8123` працює.
- `systemd-tmpfiles-clean` стирає `/tmp/deploy.sh` і `/tmp/deploy` посеред
  сесії. `allowlist_external_dirs` вимагає існуючих каталогів — точку
  монтування треба створити до `check_config`.
- Сторінка «Свердловина» на NAS-дашборді: бейджі надто дрібні (власник).

**Зроблено** (коміти в `main`)

- `92fb80c` recorder: `purge_keep_days` 7 → 21, `auto_repack: false`, другий
  зріз exclude; ~65 МБ/добу, 21 доба ≈ 1,4 ГБ на розділі 2,4 ГБ.
- `f9b12bb` відео поза деревом конфігу: галерея через підписані media-URL
  (`scripts-gen-gallery.py` тепер у Git), `sensor.motion_clips_list` знято,
  новий `mnt-homemate_media-video.mount` + symlink `media/video`,
  allowlist із реальним шляхом, `finish-video-move.sh` для власника,
  `backup_statistics.py` → `MB35x8/statistics`, `deploy.sh` у репо
  (`board-config/scripts/deploy.sh`, на платі `/home/forlinx/deploy.sh`).
- `8e31456`, `86072e6` аудит графіків і виправлення всіх сторінок.
- `7c63825` блекаут: «Світло - з'явилось - увімкнути назад» (мережа є 5 хв →
  вмикає лише те, що вимкнула автоматика; PRANA через `rest_command.prana_start`),
  насос свердловини вимикається лише нижче `input_number.nasos_sverdlovyny_porih_batarei`
  (10 %), «вимкнути важкі» закриває сесію зарядки авто, розклади бойлера бабусі
  та зарядки без світла не стартують і стартують із запізненням, сторож
  готовності знає нові id; «Свердловина» — бейджі → ряд високих плиток.
- `de72bfe` hero «Блекауту» без волатильних чисел, шкала й плитки підписані
  джерелом; правило в [dashboard-layout.md](dashboard-layout.md).
- `9ee9f0a` drop-in `home-assistant.service.d/wait-for-clock.conf` (HA чекає
  до 5 хв на рік ≥ 2026; встановлено на платі), запис бану прибрано з
  `ip_bans.yaml`, `deploy.sh reload` з запасною LAN-адресою.
- Вручну на платі: усі вимкнені о 10:34 прилади увімкнено назад об 11:20;
  копію `minute_rollup.db.broken-20260828` винесено в `/userdata/hass/tmp/broken/`.

**Рішення власника (03.09.2026)**

- «Збільши зберігання історії» → 21 доба; статистика безстрокова вже є;
  сирі стани на роки — лише зовнішня БД на NAS, не рекомендовано через
  залежність від NAS під час відключень.
- Відео зберігати окремо від телеметрії — так і є (NAS `ha-motion` проти бази
  на платі); вважати правильним.
- «Після появи світла вмикати назад усі споживачі», «насос свердловини має
  качати, вимикати лише при 5–10 % батареї», «важкі споживачі розшифрувати»,
  розклади бойлера бабусі й зарядки на «Блекауті» пояснити і зробити безпечними.
- Усі знахідки нотувати в журнал робіт, оновлювати скіл.

**Чекає**

- Власник: `sudo bash /home/forlinx/finish-video-move.sh` — переносить маунт
  відео, ставить nas-mounts.service, прибирає бан localhost і рестартує HA
  (заразом застосуються recorder 21 доба і allowlist). Після цього: архів 04.09
  має важити десятки МБ; HA сам залишить 7 копій, старі обірвані архіви
  (~127 ГБ) видаляти лише за згодою власника; `ha_admin.py orphans --remove`
  для залишку `sensor.motion_clips_list`.
- Причина просідання живлення плати й насоса 12:50–13:58 — з'ясувати фізично;
  плата має бути на захищеній лінії.
- Yasno «за адресою» — потрібні вулиця й будинок.
- Тестові графіки «(тестування)» — підтвердити або зняти через тиждень.

## 2026-09-02

**Знахідки.** ФС `/userdata` без журналу знову з помилками (кожне
знеструмлення псує); Deye-логер `.179` і розетки `.26`/`.82` офлайн; нічний
бекап 5–7 ГБ; 13 автоматизацій-привидів і `initial:` на помічниках скидали
налаштування після рестарту («злітають автоматизації»); журнал сповіщень існував,
але не був на дашборді; MCP-інструменти падали по таймауту, бо дочірній `ssh`
успадковував stdin JSON-RPC сервера.

**Зроблено.** Привиди прибрано, `initial:` знято, канали cloud/local як
template-сенсори з бейджами і вкладками «Сповіщення»/«Зв'язок», журнал сповіщень
із дедупом у панелі дзвіночка (`nl_<md5>`), сторінка «Блекаут», Yasno група 3.1
через API майстра, storage-дашборди в Git через `lovelace_push.py`, MCP async +
чесний `health`, ТЗ на нагрів бойлерів від сонця
([solar-boilers-spec.md](solar-boilers-spec.md)). 03.09 09:06 —
`fsck-userdata.sh` v2: ext4 з журналом, fstab `errors=remount-ro,nofail`.

## 2026-08-22

Аудит стану після переїзду на standalone-конфіг: дрейф репо ↔ плата, ФС
потребувала fsck, RAM 1,93 ГБ (не 3,8), телеметрія насоса — `scan_interval`,
не залізо. Деталі — у пам'яті асистента і в `handoff/`.
