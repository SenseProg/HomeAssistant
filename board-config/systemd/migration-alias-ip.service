[Unit]
Description=Temporary second address so the new RK3568 board on the factory subnet is reachable
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c "ip addr add 192.168.0.199/24 dev eth1 2>/dev/null; true"

[Install]
WantedBy=multi-user.target
