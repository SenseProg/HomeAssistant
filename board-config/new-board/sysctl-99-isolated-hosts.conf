# The three hosts routed through the spare board sit in the same subnet as the
# relay, so the kernel would happily accept an ICMP redirect telling it to go
# direct - back to the path that does not work. Measured 2026-08-14.
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.eth0.accept_redirects = 0
